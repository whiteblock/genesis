DEPENDENCIES
* mysql



HOW TO INSTALL

`cd $GOPATH/src`

`mkdir bitbucket.org`

`cd bitbucket.org`

`mkdir whiteblockio`

`cd whiteblockio`

`git clone https://bitbucket.org/whiteblockio/vyos.git`

`tar xfz vyos.tar.gz`

`cd ~/back_end/Docker_Deploy`

`go get`

`go build



HOW TO USE	
To run the default, which is currently syscoin

./Docker_Deploy [-i <image>] [-n <number of nodes>] [--servers <server1,server2...>] [--ethereum | --syscoin | --eos] [--verbose]
