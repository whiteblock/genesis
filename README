HOW TO INSTALL

cd $GOPATH/src

mkdir bitbucket.org

cd bitbucket.org

mkdir whiteblockio

cd whiteblockio

wget http://ns2/vyos.tar.gz

tar xfz vyos.tar.gz

cd ~/back_end/Docker_Deploy

go get

go build



HOW TO USE	
To run the default, which is currently syscoin

./Docker_Deploy -i <image> -n <number of nodes> --servers <server1,server2...>

To run ethereum or syscoin

./Docker_Deploy -i <image> -n <number of nodes> [--ethereum | --syscoin]```